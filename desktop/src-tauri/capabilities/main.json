{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the packaged main window can call the P0 commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
