# Tauri rewrites Cargo.toml with LF. Keep its checkout stable on Windows too.
*.toml text eol=lf
LICENSE text eol=lf
# Release manifests compare dependency lock bytes across Windows, macOS and Linux.
*.lock text eol=lf
package-lock.json text eol=lf
